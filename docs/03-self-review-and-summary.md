# 自检与总结

## 评分

9.6 / 10

## 扣分原因

- 还没有执行一个由你另行提供的、不同类型的真实复杂任务 forward test。
- 当前这一轮验证了结构、安装和就绪状态，但还没有验证它在第二个真实任务上的完整行为表现。

## 自检后已做修订

1. 对可复用 Skill 运行了结构校验。
2. 根据校验结果修复了 frontmatter `description` 中不允许出现尖括号的问题。
3. 再次运行校验并确认 Skill 结构有效。
4. 通过符号链接将 Skill 安装到本地 Codex Skill 目录，保证项目目录仍是唯一事实来源。

## 目前的问题是什么

此前还没有一个可复用、显式、成体系的工作流 Skill，能够把复杂任务从需求澄清、共识确认、执行指令生成、任务执行、自检到总结完整串起来。

## 本质原因是什么

这套流程原本更多存在于你个人的协作习惯和元提示词里，但还没有被封装成一个具备触发规则、参考文档、安装方式和项目产物结构的正式 Skill。

## 具体解决方案是什么

已将 `complex-task-orchestrator` 构建为一个面向复杂任务的流程型 Skill，并把当前项目组织为：

- `docs/`：本次运行的项目文档
- `skill/complex-task-orchestrator/`：可复用 Skill 本体

目前已交付：

- 需求共识文档
- 一次性任务执行指令文档
- 一份包含触发逻辑、流程规则、歧义处理和自检规则的 `SKILL.md`
- 三份支撑性 reference 文档：文档约定、执行指令模板、自检清单
- 更新后的 `agents/openai.yaml`
- 已安装到 `/Users/boyzcl/.codex/skills/complex-task-orchestrator` 的本地 Skill

## 当前方案的信心

91 / 100

## 信心说明

我对当前结构、触发语言和流程设计有较高信心，认为它已经足以进入真实使用阶段。之所以不是更高，是因为还缺少你指定的另一类真实复杂任务来做完整 forward test，因此泛化能力的验证还未完成。
