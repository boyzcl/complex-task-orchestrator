# T4 Consistency Review Note

日期：2026-04-17

快速结论：核心决策文档已经基本对齐当前口径，主要漂移集中在“入口页 / 交接页 / 历史计划页”。如果不收口，这些文档会继续把 `Claude Code` 补跑误读成当前阻塞项。

## 发现 1

`docs/evals/round-02-client-validation-plan.md:44-51` 仍把 “四个任务位都完成评分卡” 写成 `Round 2` 的最小完成条件，而 `A1 / A2` 正是 `Claude Code` 任务位。这个表述会直接把当前版本判断重新绑定到跨客户端完成度上，与“`Codex` 验证通过即可成立”的现口径冲突。继续保留这条退出条件，后续任何人按计划页执行时，都会倾向把 `Claude Code` 未补跑解释为“当前轮未完成”。

最小修正建议：在该文档顶部补一条“历史计划 / 已被 2026-04-17 当前决策降级”的说明，或把退出条件改成“当前版本以 `Codex` 任务位成立为准，`Claude Code` 为增强项补跑”。

## 发现 2

`docs/evals/session-handoff-2026-04-17.md:12-16` 和 `:79-93` 仍把“解除 `Claude Code` 环境阻塞 -> 补跑 `A1 / A2` -> 再判断是否成立”写成新对话里的优先主线。交接文档通常会被当成操作入口，因此它的优先级表述比一般记录更容易覆盖掉后面的决策文档。结果是接手者会先处理环境补跑，而不是先接受“当前版本已经可按 `Codex` 收口”的结论。

最小修正建议：把 handoff 顶部改成“双层状态”，先写“当前版本已按 `Codex` 成立”，再把 `Claude Code` 补跑下沉为“下一阶段增强 / 跨客户端扩展”。

## 发现 3

`docs/evals/README.md:42-47` 的“下一步”仍写成“先完成 Claude Code 环境解除阻塞，再完成跨客户端 Round 2 验证”。README 是目录级入口页，很多读者只看这里就会形成整体判断，所以这里的旧口径会放大文档集内部的不一致。即使 `round-02-assessment.md` 和 `ai-native-decision-2026-04-17.md` 已经收口，入口页仍会把项目描述成“尚未解除阻塞”。

最小修正建议：把“下一步”改成“当前版本已按 `Codex` 验证通过收口；若做增强，再补 `Claude Code` 跨客户端验证”，并把 `claude-code-unblock-checklist.md` 明示为可选后续项。
